# src/hooks (reserved)

Not implemented yet.

Reserved for shared, cross-feature React hooks (e.g. TanStack Query hooks
wrapping the future API client in `src/api`). No hook depending on the
backend exists yet — Sprint 0 configures the TanStack Query provider
only (`src/lib/queryClient.ts`), with nothing to query.
