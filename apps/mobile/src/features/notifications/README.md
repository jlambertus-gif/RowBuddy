# src/features/notifications (reserved)

Not implemented yet.

Reserved for push-notification registration/handling (ADR-028
Decision 6 — device-token registration against `packages/Notifications`'
new `device_tokens` table, delivered via Expo's push service). Not an
in-app notification inbox — that remains explicitly out of scope per
ADR-028. Scoped for Sprint 6.
