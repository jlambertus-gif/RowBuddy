# src/features/ratings (reserved)

Not implemented yet.

Reserved for rating submission/viewing, against the new
`/api/v1/transfers/{id}/ratings` surface ADR-028 Decision 3 introduces
(wrapping the already-existing `RatingSubmissionService`/
`RatingRevealEvaluator`, zero new domain logic). Scoped for Sprint 4.
