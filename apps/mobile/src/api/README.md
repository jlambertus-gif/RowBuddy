# src/api (reserved)

Not implemented yet.

Reserved for the hand-written TypeScript API client (ADR-028 Decision 1)
that will consume RowBuddy Web's `/api/v1` surface (ADR-028 Decision 2/3)
once Sprint 1 introduces it. Sprint 0 deliberately does not consume the
backend — this directory stays empty until there is a real, versioned
endpoint to call.
