# src/types (reserved)

Not implemented yet.

Reserved for hand-written TypeScript types mirroring the backend's
actual DTO shapes (ADR-028 Decision 1 — no OpenAPI spec exists on the
backend to generate from). Populated alongside `src/api` as each backend
surface is actually consumed, starting Sprint 1.
