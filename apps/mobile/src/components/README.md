# src/components (reserved)

Not implemented yet.

Reserved for shared, cross-feature UI components. Sprint 0 introduces no
screens and no components — see `app/` for the minimal placeholder route
used only to validate the toolchain.
