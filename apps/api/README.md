# apps/api (reserved)

Not implemented yet.

This directory is reserved for a future headless, versioned API — the kind
of surface a native mobile app or a third-party integration would need,
where Inertia's tightly-coupled request/response model doesn't fit.

It intentionally stays empty until a second real API consumer exists.
Building it earlier would be speculative infrastructure with nothing to
validate it against.
